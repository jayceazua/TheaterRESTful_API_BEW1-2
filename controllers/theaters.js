const express = require('express');
const router = express.Router();

// CREATE - post a new
router.post('', () => {

});
// READ
router.get('/:id', () => {

});
// UPDATE - update
router.put('/:id', () => {

});
// DELETE
router.delete('/:id', () => {

});

module.exports = router;
